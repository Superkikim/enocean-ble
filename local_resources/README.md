# Local Resources (Not Tracked)

This folder is local-only by policy.

Use it for:
- Home Assistant local Docker setup
- BLE captures and logs
- Experimental scripts and scratch files

Git policy:
- `local_resources/**` is ignored in git.
- Only this README is tracked to document policy.
